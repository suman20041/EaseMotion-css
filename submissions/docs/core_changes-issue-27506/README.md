# ease-fab — Floating Action Button (FAB) Speed Dial Menu

## What does this do?

A React component for a floating action button that expands into a speed dial menu with multiple actions and labels.

## How is it used?

```jsx
import FabSpeedDial from './FabSpeedDial';

function App() { return <FabSpeedDial />; }
```
